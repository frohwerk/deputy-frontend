import { UnassignedPipe } from './unassigned.pipe';

describe('UnassignedPipe', () => {
  it('create an instance', () => {
    const pipe = new UnassignedPipe();
    expect(pipe).toBeTruthy();
  });
});
